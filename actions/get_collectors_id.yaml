---
# Action definition metadata
name: "get.collectors.id"
description: "Get ID or IDs of collectors by their name. Returns array of IDs."
runner_type: "python-script"
enabled: true
entry_point: "get_collectors_id.py"
parameters:
  keyword:
    type: "string"
    required: true
    description: "Name of the collectors to search for."
